<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GTRadio" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/yellow_500</item>
        <item name="colorPrimaryVariant">@color/yellow_600</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/red_800</item>
        <item name="colorSecondaryVariant">@color/red_300</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="titleTextColor">@android:color/black</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.GTRadio.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.GTRadio.AppBarOverlay" parent="ThemeOverlay.AppCompat.ActionBar" />

    <style name="Theme.GTRadio.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- All of this just to ensure the action bar on Settings has black-on-primary text -->
    <style name="Theme.GTRadio.PreferencesTheme" parent="Theme.GTRadio">
        <item name="android:textColorPrimary">@color/black</item>
        <!-- Set the theme for the action bar -->
        <item name="actionBarTheme">@style/Theme.GTRadio.PreferencesTheme.ActionBarTheme</item>
    </style>

    <style name="Theme.GTRadio.PreferencesTheme.ActionBarTheme" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <!-- Set color of title of action bar -->
        <item name="android:textColorPrimary">@color/black</item>
    </style>

</resources>