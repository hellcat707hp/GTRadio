<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/FirstFragment">

    <fragment
        android:id="@+id/FirstFragment"
        android:name="com.sc.gtradio.GroupList"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_grouplist">
    </fragment>

    <fragment
        android:id="@+id/SecondFragment"
        android:name="com.sc.gtradio.RadioList"
        android:label="@string/radio_list_label"
        tools:layout="@layout/radio_list">
        <argument
            android:name="stationGroupId"
            app:argType="string" />
    </fragment>
</navigation>